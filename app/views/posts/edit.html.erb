<div class="main posts-new">
  <div class="container">
    <h1 class="form-heading">編集する</h1>
    <%= form_tag("/posts/#{@post.id}/update", class: "post-form") do %>
      <div class="form editform">
        <div class="form-body">
          <% @post.errors.full_messages.each do |message| %>
            <div class="form-error">
              <%= message %>
            </div>
          <% end %>
          <label class="br2-label">(1) 仕事で感じたこと</label>
          <textarea class="auto-save" name="work_content"><%= @post.work_content %></textarea>
          <label class="br2-label">(2) (1)の深掘り（解析・分析）</label>
          <textarea class="auto-save"  name="content"><%= @post.content %></textarea>
          <label class="br2-label">(3) 自己学習について</label>
          <textarea class="auto-save"  name="study_content"><%= @post.study_content %></textarea>
          <label class="br2-label">(4) 特記事項</label>
          <textarea class="auto-save"  name="notices_content" ><%= @post.notices_content %></textarea>
          <input type="submit" value="保存">
        </div>
      </div>
    <% end %>
  </div>
</div>
<input class="hidden url" value="<%= "/posts/#{@post.id}/update" %>">
<%= javascript_include_tag "posts.js" %>
<%= javascript_include_tag "urlchange.js" %>

